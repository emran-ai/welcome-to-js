'use strict';

let backwards = 'tpircsavaj';

// use a for-of loop to reverse `backwards`
//  assign the reversed string to `language`
let language = backwards.split('').reverse('').join('')

// log each character in `language`

for (let char of language) {
  console.log(char);
}
